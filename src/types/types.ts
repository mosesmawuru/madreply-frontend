/**
 * @define Components Props
 */

// MenuButtonProps
export interface MenuButtonProps {
  onClick?: any;
  flag?: boolean;
}

// ButtonProps
export interface ButtonProps {
  label: any;
  onClick: any;
}

// -------------------------------------//

/**
 * @define styled-Components Props
 */

// ScrollTopProps

export interface ScrollButtonProps {
  showButton: boolean;
  [key: string]: unknown;
}
